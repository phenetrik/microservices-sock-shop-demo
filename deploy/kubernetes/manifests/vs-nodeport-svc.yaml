apiVersion: v1
kind: Service
metadata:
  name: nginx-ingress-controller
  namespace: ingress-nginx
spec:
  type: NodePort
  selector:
    app: ingress-nginx
    component: controller
  ports:
    - name: http
      port: 8088
      targetPort: http
      protocol: TCP

